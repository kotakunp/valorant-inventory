[phases.setup]
nixPkgs = ["nodejs_20", "chromium", "xorg.xvfb"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
# Headed chromium under Xvfb — headless trips Riot/Akamai and can freeze screenshots.
cmd = "xvfb-run -a npm start"
